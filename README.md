
# Nexus OSINT Platform (情报分析与搜集平台)

![Status](https://img.shields.io/badge/Status-Active-success)
![Version](https://img.shields.io/badge/Version-5.2-blue)
![Tech](https://img.shields.io/badge/Tech-React19%20%7C%20Gemini%20AI%20%7C%20Tailwind-0ea5e9)
![Platform](https://img.shields.io/badge/Platform-Web%20%7C%20Windows%20%7C%20macOS-lightgrey)

**Nexus** 是一个专业级的开源情报 (OSINT) 分析平台。它结合了无限画布的可视化调查能力、基于图谱的数据关联分析以及 Google Gemini AI 驱动的自动化推理引擎。旨在帮助情报分析师、安全研究员和调查人员高效地进行信息的搜集、整理、分析与决策。

---

## 📚 文档目录 (Table of Contents)

1. [项目简介 (Introduction)](#1-项目简介)
2. [快速开始 (Quick Start)](#2-快速开始)
3. [📦 客户端打包 (Desktop App)](#3-📦-客户端打包)
4. [📘 用户使用手册 (User Manual)](#4-📘-用户使用手册)
5. [🏗️ 系统架构文档 (Architecture)](#5-🏗️-系统架构文档)
6. [🛠️ 技术开发文档 (Technical Docs)](#6-🛠️-技术开发文档)

---

## 1. 项目简介

Nexus 将传统的“白板墙”调查数字化，并赋予了 AI 智能。

### 核心特性
*   **可视化情报图谱**: 支持拖拽、缩放、连线的无限画布，自动布局复杂关系网。
*   **多维实体支持**: 内置 60+ 种情报实体类型（HUMINT、SIGINT、网络资产、物理设施、加密货币等）。
*   **AI 增强分析**: 集成 Google Gemini 2.5/3.0 模型，支持深度思考 (Thinking Mode)。
*   **情报搜集工具箱**:
    *   **Agent**: 心理侧写、代码审计、翻译解码。
    *   **MCP (Live)**: 实时 Google 搜索 grounding，支持 GitHub, LinkedIn, WHOIS, 漏洞库等实时数据查询。
    *   **专业搜集**: 包含 HUMINT 信源评级、SIGINT 频谱分析、IMINT 图像侦察等专业模组。
*   **决策辅助**: 自动化生成时间线 (Timeline) 和情报简报 (Briefing)。
*   **自定义扩展**: 内置无代码插件编辑器，可随时创建新的分析工具。

---

## 2. 快速开始

### 环境要求
*   Node.js 18+
*   Google Gemini API Key (必须开通 Paid Tier 以使用搜索 Grounding 功能)

### 安装与运行

1.  **克隆项目**
    ```bash
    git clone https://github.com/your-repo/nexus-osint.git
    cd nexus-osint
    ```

2.  **安装依赖**
    ```bash
    npm install
    ```

3.  **配置环境**
    在运行前，请确保设置了环境变量 `API_KEY`。通常在开发环境下，可以通过 `.env` 文件或启动命令注入。

4.  **启动开发服务器**
    ```bash
    # Linux/Mac
    export API_KEY="your_google_api_key_here"
    npm start

    # Windows (PowerShell)
    $env:API_KEY="your_google_api_key_here"
    npm start
    ```

---

## 3. 📦 客户端打包

Nexus 支持被打包为独立的桌面应用程序（.exe / .dmg / .AppImage），以获得更好的性能和系统集成。

我们使用 **Electron** 进行封装。详细步骤请参考项目中的打包指南：

👉 **[点击查看：BUILD_GUIDE.md (客户端打包指南)](./BUILD_GUIDE.md)**

简要命令：
```bash
# 1. 安装 Electron 依赖
npm install electron electron-builder --save-dev
npm run build

# 2. 调试桌面版
npm run electron:dev

# 3. 构建安装包
npm run electron:build
```

---

## 4. 📘 用户使用手册

本手册适用于情报分析师和调查人员。

### 4.1 界面概览
*   **中央画布 (Canvas)**: 您的主要工作区。按住鼠标左键或空格键可拖动画布，滚轮缩放。
*   **左上角**: 全局搜索栏。输入关键词可高亮实体，或输入指令。
*   **右侧控制台 (Control Panel)**:
    *   **🛡️ 资产库**: 添加新实体，查看所有可用工具插件。
    *   **📅 时间线**: 自动提取数据中的时间信息，按顺序排列事件。
    *   **⚙️ 属性面板**: 编辑选中节点的详细字段，查看 AI 分析结果。
    *   **🕒 日志**: 查看系统操作记录和 AI 执行状态。
    *   **🎛️ 设置**: 调节 AI 模型版本、温度和 Token 预算。

### 4.2 基础操作
*   **创建实体**: 点击右侧“资产库”中的图标（如“IP地址”、“人员目标”），实体将出现在画布中央。
*   **建立关系**:
    1.  鼠标悬停在实体节点上，右侧会出现一个“连接点 (Link Icon)”。
    2.  拖拽连接点到另一个实体上，即可建立连线。
*   **移动与布局**: 直接拖拽节点调整位置。点击右侧面板顶部的 **布局图标 (Layout)** 可一键自动整理杂乱的图谱。
*   **删除**: 选中节点（支持 Shift 多选），按 `Delete` 或 `Backspace` 键，或使用右键菜单。

### 4.3 使用 AI 工具与搜集插件
Nexus 的核心在于工具的使用：
1.  **右键菜单**: 右键点击任意节点，菜单会列出适用于该类型实体的工具。
    *   *例：右键点击一个“域名” -> 选择“Whois 查询”。*
2.  **控制面板运行**: 选中节点后，在右侧属性面板中点击“绑定插件”列表中的工具。
3.  **查看结果**:
    *   工具运行后，节点状态会变为 `PROCESSING`。
    *   完成后，可能会生成新的关联节点（自动扩展图谱），或更新当前节点的属性。
    *   查看右侧日志面板确认执行详情。

### 4.4 高级功能
*   **自定义工具**: 在资产库面板点击 `+` 号。您可以编写自己的 Prompt，或者配置一个新的 API 接口模拟器。
*   **情报简报**: 切换到“时间线”标签页，点击底部的“生成情报简报”，AI 将汇总当前画布上的所有信息，撰写一份专业的分析报告，支持导出 TXT。
*   **文件导入**: 支持导入 JSON 格式的图谱数据，或将文本文档直接拖入作为 `DOCUMENT` 节点进行分析。

---

## 5. 🏗️ 系统架构文档

### 5.1 架构设计理念
Nexus 采用 **Client-Side Heavy (重客户端)** 架构。所有的图谱渲染、状态管理和轻量级逻辑均在浏览器端完成。AI 推理通过 Google Gemini API 直接调用，无需中间后端服务（Serverless 风格）。

### 5.2 核心模块图
```mermaid
graph TD
    A[App Container] --> B[Canvas Engine]
    A --> C[Control Panel]
    A --> D[Gemini Service]
    
    B --> E[Node Rendering]
    B --> F[Connection Rendering (SVG)]
    B --> G[Interaction Handler (Pointer Events)]
    
    C --> H[Tool/Plugin Manager]
    C --> I[Timeline Engine]
    C --> J[Log System]
    
    D --> K[Google GenAI SDK]
    K --> L[Gemini Pro/Flash Model]
    K --> M[Google Search Grounding]
```

### 5.3 数据流 (Data Flow)
1.  **用户交互**: 用户在 Canvas 上触发操作（如运行工具）。
2.  **状态更新**: React `useState` 更新节点状态为 `PROCESSING`。
3.  **服务调用**: `executeTool` 函数被调用，它根据工具类型（Agent/API/MCP）构建 Prompt。
4.  **AI 推理**: 请求发送至 Google Gemini。
    *   *Agent*: 纯 LLM 推理。
    *   *MCP*: 触发 Google Search 工具，获取真实 Web 数据。
5.  **图谱扩展**: AI 返回结构化 JSON（包含 `new_entities`, `updated_properties`）。
6.  **渲染更新**: App 解析 JSON，将新节点和连线注入 `nodes` 和 `connections` 状态数组，界面自动重绘。

---

## 6. 🛠️ 技术开发文档

### 6.1 技术栈
*   **Framework**: React 19
*   **Language**: TypeScript
*   **Styling**: Tailwind CSS
*   **AI SDK**: `@google/genai` (v1.30.0+)
*   **Icons**: `lucide-react`
*   **Build Tool**: (假设为 Webpack 或 Vite，当前环境为即时编译)

### 6.2 核心数据结构 (`types.ts`)

#### IntelNode (情报节点)
```typescript
interface IntelNode {
  id: string;
  type: NodeType; // 枚举类型，如 IP_ADDRESS, ENTITY
  title: string;
  data: Record<string, string>; // 灵活的 KV 存储
  position: { x: number, y: number };
  status: 'NEW' | 'PROCESSING' | 'PROCESSED' | 'ERROR';
  // ...
}
```

#### Tool (插件/工具)
```typescript
interface Tool {
  id: string;
  category: 'AGENT' | 'API' | 'MCP';
  targetTypes: NodeType[]; // 该工具适用的实体类型
  promptTemplate: string;  // 发送给 AI 的指令模版
  mcpConfig?: { functionName: string }; // 用于 Search Grounding
  // ...
}
```

### 6.3 AI 服务层 (`services/geminiService.ts`)
这是系统的“大脑”。它实现了策略模式来处理不同类型的工具：

1.  **Prompt 构建**: 将节点数据 (`node.data`) 序列化并注入到 Prompt 模版中。
2.  **Schema 强制**: 使用 `responseSchema` 强制 Gemini 返回严格的 JSON 格式，包含 `new_entities` 数组，确保图谱可以自动生成。
3.  **Search Grounding**: 当检测到 `ToolCategory.MCP` 且函数名为 `googleSearch` 时，会自动配置 `tools: [{googleSearch: {}}]`，使模型能够访问实时互联网。

### 6.4 扩展开发指南

#### 如何添加新的实体类型？
1.  修改 `types.ts` 中的 `NodeType` 枚举。
2.  在 `constants.ts` 中的 `ENTITY_DEFAULT_FIELDS` 添加该类型的默认字段结构。
3.  在 `NodeCard.tsx` 的 `getIcon` 函数中添加对应的图标。
4.  在 `ControlPanel.tsx` 的分类列表中注册该类型按钮。

#### 如何硬编码一个新的内置工具？
1.  打开 `tools.ts`。
2.  在 `DEFAULT_TOOLS` 数组中添加一个新的 `Tool` 对象。
3.  **Prompt 编写技巧**: 明确要求 AI 输出 JSON，并定义好你希望提取的信息字段。
    *   *示例*: "分析输入文本，提取其中的所有 IP 地址，并判断其归属地。"

---

**Nexus OSINT Platform** - *Empowering Intelligence with AI.*
